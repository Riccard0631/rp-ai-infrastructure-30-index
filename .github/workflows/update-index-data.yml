name: Update RPAI30 index data

on:
  schedule:
    # Weekdays after the U.S. market close. GitHub cron is UTC.
    - cron: "30 22 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-rpai30-index-data
  cancel-in-progress: false

jobs:
  update-index-data:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install Python dependencies
        run: python -m pip install -r requirements.txt

      - name: Run daily index update
        run: python scripts/run_daily_update.py

      - name: Commit refreshed data
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(data): update RPAI30 index data"
          file_pattern: "history.csv backtest_history.csv backtest_summary.json data_status.json"
